version: "2.3"
services:
  nodeserver:
    image: "sylchauf/smart-kotkot:latest"
    restart: always
    ports:
      - "80:3000"
    device_cgroup_rules:
      - 'c *:* rmw'
    volumes:
      - ./state:/app/state
      - /dev:/dev
